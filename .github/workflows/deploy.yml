- name: Remote SSH
    uses: appleboy/ssh-action@master
    with:
      host: "115.175.37.0" # 👈 直接写死 IP 字符串，看它还报不报 missing host
      username: "root"      # 👈 直接写死用户名
      key: ${{ secrets.SERVER_SSH_KEY }}
      port: 22
      script: |
        cd /root/www/my-geek-blog
        git pull origin master
        npm run build
        pm2 restart my-blog